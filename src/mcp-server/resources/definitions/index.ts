/**
 * @fileoverview Barrel — collects all resource definitions into `allResourceDefinitions`.
 * @module mcp-server/resources/definitions/index
 */

import { surfaceResource } from './surface.resource.js';

export const allResourceDefinitions = [surfaceResource];
